# Rust
/target/
**/target/
Cargo.lock.bak
*.rs.bk

# Fuzz artefacts
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# Temp
*.swp
*.swo
*~

# Python venv for fixture generation
.venv/

# Node deps for kdbxweb fixture generator (on a separate branch in the
# public repo as well — kept in .gitignore here so accidental runs of
# `npm install` from this tree never commit into feature branches)
/tests/fixtures/.node/node_modules/
/tests/fixtures/.node/package-lock.json
